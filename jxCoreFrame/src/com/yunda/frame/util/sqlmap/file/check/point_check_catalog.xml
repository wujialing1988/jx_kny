<?xml version="1.0" encoding="UTF-8"?>
<SqlMap>
	<sql id="queryPageList">
		SELECT
			B.*, A.EQUIPMENT_CODE,
			A.EQUIPMENT_NAME,
			A.CLASS_CODE,
			A.CLASS_NAME,
			A.USE_PLACE,
			A.USE_WORKSHOP,
			A.USE_WORKSHOP_ID,
			A.USE_PERSON,
			A.USE_PERSON_ID,
			A.MECHANICAL_REPAIR_TEAM,
			A.MECHANICAL_REPAIR_TEAM_ID,
			A.MECHANICAL_REPAIR_PERSON,
			A.MECHANICAL_REPAIR_PERSON_ID,
			A.ELECTRIC_REPAIR_TEAM,
			A.ELECTRIC_REPAIR_TEAM_ID,
			A.ELECTRIC_REPAIR_PERSON,
			A.ELECTRIC_REPAIR_PERSON_ID,
			A.RUNING_SHIFTS,
			A.MODEL,
			A.SPECIFICATION,
			A.MANAGE_CLASS,
			A.MANAGE_LEVEL,
			A.FIXED_ASSET_NO,
			A.ORG_ID,
			A.ORG_NAME,
			A.ELECTRIC_COEFFICIENT,
			A.MECHANICAL_COEFFICIENT
		FROM
			E_EQUIPMENT_PRIMARY_INFO A,
			E_SBJX_POINT_CHECK_CATALOG B
		WHERE
			A.IDX = B.EQUIPMENT_IDX
		AND B.RECORD_STATUS = 0
	</sql>
	<sql id="statisticsByDaily">
		SELECT
			A.IDX,
			C.EQUIPMENT_CODE,
			C.EQUIPMENT_NAME,
			C.USE_PERSON,
			B.CHECK_DATE,
			B.CHECK_EMP,
			B.STATE,
			B.CAL_RUNNING_TIME,
			B.RUNNING_TIME
		FROM
			E_SBJX_POINT_CHECK_CATALOG A
		LEFT JOIN (
			SELECT
				*
			FROM
				E_SBJX_POINT_CHECK
			WHERE
				TO_CHAR(CHECK_DATE, 'YYYY-MM-DD') = '2017-03-24'
			AND RECORD_STATUS = 0
		) B ON A.EQUIPMENT_IDX = B.EQUIPMENT_IDX
		LEFT JOIN E_EQUIPMENT_PRIMARY_INFO C ON A.EQUIPMENT_IDX = C.IDX
		WHERE
			A.RECORD_STATUS = 0
		ORDER BY C.EQUIPMENT_CODE
	</sql>
</SqlMap>